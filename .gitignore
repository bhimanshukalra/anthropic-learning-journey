# secrets — never commit
.env

# generated / machine-specific
__pycache__
.venv
.DS_Store

# Claude Code local overrides (commands/ stay tracked)
.claude/settings.local.json

# personal planning workspace — keep the entire folder off the public repo
planning/
